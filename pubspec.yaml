name: pocket_ledger
description: "PocketLedger - Premium Fintech Expense Tracker"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.6.0
  flutter_riverpod: ^2.6.1
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  fl_chart: ^0.70.2
  flex_color_scheme: ^8.3.1 # Ensure this is the latest to use defaultRadius
  animations: ^2.0.11
  intl: ^0.20.2
  uuid: ^4.5.1
  flutter_hooks: ^0.20.5
  hooks_riverpod: ^2.6.1
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.13

flutter:
  uses-material-design: true
